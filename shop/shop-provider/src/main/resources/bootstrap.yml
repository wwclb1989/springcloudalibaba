server:
  port: 8081

spring:
  application:
    name: shop-provider
  profiles:
    active: dev

# 配置中心nacos-config
  cloud:
    nacos:
      config:
        server-addr: 192.168.47.129:8848
        file-extension: yml
