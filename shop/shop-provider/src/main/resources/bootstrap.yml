server:
  port: 8081

spring:
  application:
    name: shop-provider
  profiles:
    active: dev

  cloud:
    nacos:
      config:
        server-addr: 192.168.47.129:8848
        file-extension: yml
