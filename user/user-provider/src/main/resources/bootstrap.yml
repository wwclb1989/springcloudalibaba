server:
  port: 8082

spring:
  application:
    name: user-provider

  profiles:
    active: dev

  cloud:
    sentinel:
      transport:
        port: 8719
        dashboard: 192.168.47.129:8080

    nacos:
      discovery:
        server-addr: 192.168.47.129:8848
      config:
        server-addr: 192.168.47.129:8848
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: '*'

feign:
  sentinel:
    enabled: true
