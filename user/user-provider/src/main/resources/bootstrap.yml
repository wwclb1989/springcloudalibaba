server:
  port: 8082

spring:
  application:
    name: user-provider
  profiles:
    active: dev

  cloud:
    nacos:
      config:
        server-addr: 192.168.47.129:8848
        file-extension: yml
