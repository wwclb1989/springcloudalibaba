server:
  port: 8080

spring:
  application:
    name: gateway-sample
  profiles:
    active: dev

  cloud:
    nacos:
      config:
        server-addr: 192.168.47.129:8848
        file-extension: yml